# CSS Variables

CSS variables (custom properties) allow you to store reusable values in CSS.

Concepts covered:

- Defining variables using :root
- Using variables with var()
- Dynamic theming
- Theme switching (light/dark)

CSS variables improve maintainability and consistency in styling.